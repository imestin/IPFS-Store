import React from 'react';

class MainArea extends React.Component {

    render() {
        return(
            <div id="mainArea" className="mainArea">
                This is the main area where the list item or the about page will be loaded.
                I put here a longer text to see what's going on.

                What's goind on?
            </div>
        );
    }
}

export default MainArea;